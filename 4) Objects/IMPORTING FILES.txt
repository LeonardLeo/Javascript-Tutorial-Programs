Importing Files

It's time to import our first file! 
Let's import the ORDER_TYPES we just created into numberOfPizzas.js.

We can use require to pull in the exports from orderType.js:

const ORDER_TYPES = require('./orderTypes');